apiVersion: v1
kind: Service
metadata:
  name: egress-webhook
  namespace: opensandbox
  labels:
    app: egress-webhook
    app.kubernetes.io/part-of: cubebox-egress
spec:
  selector:
    app: egress-webhook
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
