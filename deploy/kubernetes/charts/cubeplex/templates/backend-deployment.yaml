apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cubeplex.backend.fullname" . }}
  labels:
    {{- include "cubeplex.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.backend.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "cubeplex.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: backend
  template:
    metadata:
      labels:
        {{- include "cubeplex.labels" . | nindent 8 }}
        app.kubernetes.io/component: backend
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/backend-configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/backend-secret.yaml") . | sha256sum }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        # Wait for postgres + run alembic migrations before the API boots.
        # Done in-pod (not as a helm pre-upgrade hook) so postgres is
        # already reachable when this fires — a hook would race against
        # the postgres StatefulSet on a fresh install.
        - name: migrate
          image: {{ include "cubeplex.backend.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - sh
            - -c
            - |
              set -e
              echo "waiting for postgres..."
              until python -c "import socket, sys; s=socket.socket(); s.settimeout(2); s.connect(('{{ include "cubeplex.postgresql.host" . }}', 5432)); s.close()" 2>/dev/null; do
                sleep 2
              done
              echo "postgres reachable; running alembic upgrade head"
              alembic upgrade head
          env:
            {{- range $k, $v := .Values.backend.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          volumeMounts:
            - name: config
              mountPath: /app/config.production.local.yaml
              subPath: config.production.local.yaml
            - name: secrets
              mountPath: /app/config.production.secrets.yaml
              subPath: config.production.secrets.yaml
              readOnly: true
      containers:
        - name: backend
          image: {{ include "cubeplex.backend.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            {{- range $k, $v := .Values.backend.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
            {{- if .Values.egress.enabled }}
            - name: mtls
              containerPort: 8443
              protocol: TCP
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /app/config.production.local.yaml
              subPath: config.production.local.yaml
            - name: secrets
              mountPath: /app/config.production.secrets.yaml
              subPath: config.production.secrets.yaml
              readOnly: true
            {{- if .Values.egress.enabled }}
            - name: egress-mtls
              mountPath: /etc/egress-mtls
              readOnly: true
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "cubeplex.backend.fullname" . }}-config
        - name: secrets
          secret:
            secretName: {{ include "cubeplex.backend.fullname" . }}-secrets
        {{- if .Values.egress.enabled }}
        - name: egress-mtls
          secret:
            secretName: {{ printf "%s-mtls" (include "cubeplex.backend.fullname" .) | quote }}
        {{- end }}
