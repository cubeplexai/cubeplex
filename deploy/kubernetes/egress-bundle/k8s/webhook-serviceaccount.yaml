# ServiceAccount for the egress admission webhook (referenced by the Deployment).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: egress-webhook
  namespace: opensandbox
  labels:
    app.kubernetes.io/part-of: cubeplex-egress
