# helm dependency build artefacts — regenerated by `helm dep update`. Source
# lives under vendor/. Operator runs `deploy/scripts/helm-install.sh`, which
# runs dep update before installing.
/Chart.lock
/charts/
