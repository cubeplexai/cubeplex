# 1686eed is cubepi main HEAD: v3 HITL run_id support — CheckpointedChannel
# persists host-side run_id alongside pending_request atomically; bumps
# cubepi schema_version 2 → 3 (matching alembic migration shipped here).
# Strictly newer than 738f1e4 (memory reflection on_run_end hook in ancestry).
cubepi = { git = "https://github.com/cubeplexai/cubepi.git", rev = "1686eed" }
