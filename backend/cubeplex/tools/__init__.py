"""Built-in tool package.

cubeloop assembles the per-run tool list in
``cubeplex.streams.run_manager._run_cubeloop_path`` via
``cubeplex.tools.registry.list_builtin_tools`` and the per-middleware
``tools`` hooks.
"""
