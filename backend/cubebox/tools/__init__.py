"""Built-in tool package.

cubepi assembles the per-run tool list in
``cubebox.streams.run_manager._run_cubepi_path`` via
``cubebox.tools.registry.list_builtin_tools`` and the per-middleware
``tools`` hooks.
"""
