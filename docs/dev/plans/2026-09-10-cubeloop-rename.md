# CubePlex cutover to CubeLoop 0.14 — plan

**Goal:** Pin CubePlex on `cubeloop` 0.14.0, migrate checkpointer tables
v5→v6, rewrite live imports/identifiers/SQL, cut Tempo over to
`cubeloop.*` attributes, and update current docs and skills.

**Architecture:** One release-shaped cutover. The pin, the Alembic
rename, the import rewrite, and the Tempo attribute rename must ship
together: opening 0.14 against v5 raises `CubeloopSchemaMismatch`, and
0.14 spans are invisible to today's `span.cubepi.*` TraceQL. Pre-0.14
traces are not dual-read. Frozen `docs/dev` snapshots and the SQL inside
historical Alembic revisions stay as they are. CubePlex does not install
the `cubepi` 0.14 shim.

**Tech stack:** Python 3.13 / `uv` / cubeloop 0.14.0 (git tag `v0.14.0`
on `cubeplexai/cubeloop`) / Postgres checkpointer schema 6 / Tempo
TraceQL / FastAPI / pytest.

Spec: [2026-09-10-cubeloop-rename-design.md](../specs/2026-09-10-cubeloop-rename-design.md).

---

## Unit 1 — Pin cubeloop 0.14.0

**Files**

- `backend/pyproject.toml` — replace the `cubepi[…]` project dep and
  the `[tool.uv.sources]` git override with `cubeloop` pointed at
  `https://github.com/cubeplexai/cubeloop.git` rev `v0.14.0`. Description
  string drops "with cubepi".
- `backend/uv.lock` — regenerated by `uv add` / `uv lock`.

**Interfaces**

```
cubeloop[mcp,postgres,trace-cli,tracing,tracing-otlp] @
  git+https://github.com/cubeplexai/cubeloop.git@v0.14.0
```

No `cubepi` extra, no shim wheel.

**Core logic**

From `backend/`:

```
uv add 'cubeloop[mcp,postgres,trace-cli,tracing,tracing-otlp] @ git+https://github.com/cubeplexai/cubeloop.git@v0.14.0'
```

If `uv add` leaves a leftover `cubepi` source or project line, delete
that line and re-lock. Inspect the lock diff: cubeloop plus its direct
transitives may move; unrelated pins should not.

0.13.6 (compaction trailing-synthetic fix) is included in 0.14. No
CubePlex code is expected for it; it is verified by the existing
compaction / conversation E2E later.

**Tests**

- `uv run python -c "import cubeloop, cubeloop.agent, cubeloop.checkpointer.postgres; import cubepi"`
  — cubeloop imports, `cubepi` raises `ModuleNotFoundError`.
- `git diff backend/uv.lock` reviewed before commit.

This unit will not boot the agent path by itself (imports still say
`cubepi`). Do not land it on main alone.

---

## Unit 2 — Alembic env.py + v5→v6 revision

**Files**

- `backend/alembic/env.py` — `cubeloop_metadata`; exclude both
  `cubepi_*` and `cubeloop_*` parents (including **threads**) and both
  partition prefixes from autogenerate.
- `backend/alembic/versions/<auto>_cubeloop_v5_to_v6_rename.py` —
  created via `--autogenerate`, body filled with the upstream helpers.
- `backend/alembic/versions/555c11215b57_add_cubepi_checkpointer_tables.py`
  — **do not** retarget `create_message_partitions_op()`. Drop that
  import and inline `CREATE TABLE cubepi_messages_p00…p63 PARTITION OF
  cubepi_messages …`. Keep `write_schema_version_op` but import it from
  `cubeloop.checkpointer.postgres.alembic_helpers`.
- v2–v5 revisions that import `upgrade_v3_to_v4_op` /
  `upgrade_v4_to_v5_op` / `write_schema_version_op` — import path only
  becomes `cubeloop.checkpointer.postgres.alembic_helpers`. Those
  helpers still emit `cubepi_*`.
- `eef196f4c8f9` — leave the `cubepi.providers.catalog` ImportError
  branch; that module never existed on cubeloop.
- `backend/tests/unit/test_alembic_env.py` — assert `cubeloop_metadata`
  and that `_CHECKPOINT_TABLES` contains both `cubepi_threads` and
  `cubeloop_threads`.

**Interfaces**

```python
from cubeloop.checkpointer.postgres.alembic_helpers import (
    upgrade_v5_to_v6_op,
    write_schema_version_op,
)

def upgrade() -> None:
    op.execute(upgrade_v5_to_v6_op())
    op.execute(write_schema_version_op())  # writes 6 into cubeloop_schema_version
```

Downgrade is the inverse of `upgrade_v5_to_v6_op()`, not a DROP. Parent
`ALTER TABLE … RENAME TO` does not rename partitions; list all 128
children. Order:

1. Rename `ix_cubeloop_messages_metadata_gin`,
   `ix_cubeloop_messages_thread_run`, `ix_cubeloop_runs_thread_seq`
   back to `ix_cubepi_*`.
2. `cubeloop_schema_version` → `cubepi_schema_version`
3. `cubeloop_hitl_answers` → `cubepi_hitl_answers`
4. `cubeloop_runs_p00…p63` then `cubeloop_runs` → `cubepi_runs*`
5. `cubeloop_messages_p00…p63` then `cubeloop_messages` → `cubepi_messages*`
6. `cubeloop_threads` → `cubepi_threads`
7. Write version 5 into `cubepi_schema_version`

`env.py` exclusion set after this unit:

- Tables: existing checkpoint leftovers plus **`cubepi_threads`**,
  `cubepi_messages`, `cubepi_runs`, `cubepi_hitl_answers`,
  `cubepi_schema_version`, **`cubeloop_threads`**, `cubeloop_messages`,
  `cubeloop_runs`, `cubeloop_hitl_answers`, `cubeloop_schema_version`.
  `cubepi_threads` is the load-bearing add: it is not excluded today,
  and after metadata is cubeloop-named a still-v5 DB would otherwise
  get `DROP cubepi_threads`.
- Prefixes: `cubepi_messages_p`, `cubepi_runs_p`, `cubeloop_messages_p`,
  `cubeloop_runs_p`.

**Core logic**

Order is load-bearing:

1. Edit `env.py` (including `cubepi_threads`) so autogen against a
   still-v5 DB does not propose `DROP cubepi_*` / `CREATE cubeloop_*`.
2. Inline v1 partition DDL in `555c11215b57` **before** the pin is
   used to run alembic on an empty DB.
3. `cd backend && uv run alembic heads` — single head; capture it.
4. `uv run alembic revision --autogenerate -m "cubeloop v5 to v6 rename"`.
   Empty body is the expected outcome. Inspect the file: no
   `DROP TABLE cubepi_threads` / no `CREATE TABLE cubeloop_*`.
5. Hand-add the two `op.execute` calls plus the reverse-rename
   downgrade. `down_revision` is the captured head.
6. Retarget v2–v5 helper imports (not v1's `create_message_partitions_op`).

`write_schema_version_op()` in 0.14 already chooses cubeloop vs cubepi
version table by which one exists. Do not rewrite historical calls to
insert version 1/2/3/4/5 by hand.

**Tests**

- Unit: `test_alembic_env.py` asserts both `cubepi_threads` and
  `cubeloop_threads` are excluded.
- Worktree **dev** DB (already at v5): `alembic upgrade head`, then
  `\dt cubeloop_*`, `\dt cubepi_*` (data tables gone), `SELECT version
  FROM cubeloop_schema_version` → `6`.
- Greenfield replay: e2e test-DB bootstrap (`alembic upgrade head` on
  an empty `cubeplex_test_*` database) is the v1→v6 replay. If v1 still
  calls `create_message_partitions_op()`, this fails first.
- Throwaway-DB round trip after the new revision exists:
  upgrade to v6, downgrade one revision, upgrade again; confirm 64+64
  partitions and version 6. Not a committed test; a command check in
  this unit.
- E2E that opens the checkpointer (`test_cubeloop_checkpointer_integration`,
  renamed in Unit 3) is the contract that `_verify_schema` accepts 6.

Placement: env.py assertion is unit. Schema presence after upgrade is
e2e (it runs alembic / hits Postgres).

---

## Unit 3 — Import + identifier rewrite

**Files**

Every live Python file under `backend/cubeplex/`, `backend/tests/`,
`backend/alembic/env.py`, and `backend/main.py` that mentions cubepi
as an import or CubePlex symbol. Highest-churn:

- `cubeplex/streams/run_manager.py` — class/function names in the spec
  table (`CubeloopAgentRunError`, `_run_cubeloop_path`,
  `cubeloop_dict_to_agent_event`, …).
- `cubeplex/mcp/cubepi_runtime.py` → `cubeloop_runtime.py` (git mv) and
  every importer (`disclosure.py`, `mcp_discovery.py`, `admin_mcp.py`,
  `ws_mcp.py`, `oauth/callback.py`, tests).
- `cubeplex/agents/{graph,convert,stream,tracing,checkpointer}.py`
- `cubeplex/llm/builder.py` and the rest of `llm/`
- `cubeplex/tools/**`, `cubeplex/middleware/**`
- Tests named `test_cubepi_*.py` / `test_run_manager_cubepi_*.py` —
  git mv to `test_cubeloop_*`.

Do **not** rewrite `docs/dev/**`. Do **not** rewrite SQL strings inside
historical Alembic revisions.

**Interfaces**

Public-to-CubePlex callables after the rename (same signatures, new
names):

```python
class CubeloopAgentRunError(RuntimeError): ...
def cubeloop_dict_to_agent_event(d: dict[str, Any], timestamp: str) -> AgentEvent | None: ...
def wire_input_to_cubeloop_user_message(...) -> UserMessage: ...
def steering_message_to_cubeloop(row: SteeringMessage) -> UserMessage: ...
async def load_workspace_mcp_tools_for_cubeloop(...) -> list[AgentTool]: ...
```

Library imports: `from cubeloop import Agent, tool` and the existing
submodule paths with the root swapped (`cubeloop.providers.base`,
`cubeloop.hitl`, `cubeloop.middleware.compaction`, …). Submodule layout
did not change in 0.14.

**Core logic**

Mechanical, but not a blind repo-wide sed: skip `docs/dev`, skip SQL in
`alembic/versions/*` except the helper import lines, skip the
`eef196f4c8f9` catalog ImportError. After the rewrite, a case-sensitive
`from cubepi` / `import cubepi` grep of `backend/cubeplex`,
`backend/tests`, and `backend/alembic/env.py` is empty except that one
migration.

**Tests**

Existing unit tests keep their assertions; they just import the new
names. Update tests that `hasattr(RunManager, "_run_cubepi_path")` or
patch `cubeplex.mcp.cubepi_runtime.*`. No new behavioural tests in this
unit — behaviour is unchanged.

Run the focused unit files that import the renamed symbols
(`test_run_manager_cubeloop_dict_to_event`, `test_graph`,
`test_alembic_env`, `tests/unit/mcp/test_runtime_*`). Full agent E2E
waits until Unit 4 (SQL) is in, otherwise history/fork/delete hit
missing `cubepi_*` tables.

---

## Unit 4 — Live SQL against renamed tables

**Files**

- `backend/cubeplex/services/history_window.py` — `cubeloop_messages`
- `backend/cubeplex/repositories/attachment.py` — `cubeloop_threads`
- `backend/cubeplex/api/routes/v1/workspaces.py` — `DELETE FROM cubeloop_threads …`
- `backend/cubeplex/api/routes/v1/auth.py` — same
- `backend/cubeplex/streams/recovery.py` — log/function names only
  (`mark_run_complete` already goes through the checkpointer)
- E2E tests that `INSERT`/`DELETE`/`SELECT` `cubepi_threads` /
  `cubepi_messages` / `cubepi_runs`

**Interfaces**

Table names after v6 (Postgres):

| Table | Partitions |
|---|---|
| `cubeloop_threads` | none |
| `cubeloop_messages` | `cubeloop_messages_p00`…`p63` |
| `cubeloop_runs` | `cubeloop_runs_p00`…`p63` |
| `cubeloop_hitl_answers` | none |
| `cubeloop_schema_version` | none |

No dual-read of table names. After the migration the old names are gone;
a leftover `FROM cubepi_messages` is a bug.

**Core logic**

String replace of the four live table identifiers in application SQL.
`ON DELETE CASCADE` from `cubeloop_threads` to messages/runs/hitl still
holds, so workspace/user delete can keep deleting only the threads row
set.

**Tests**

- E2E: history window, checkpointer round-trip, conversation fork,
  stranded-run recovery, workspace/user delete paths that currently
  mention `cubepi_threads`. These already exist; they fail until this
  unit if Unit 2 has been applied.
- Invariant: bootstrap of a conversation still returns the same
  pydantic-dump messages; `seq` is still `cubeloop_messages.seq`.

---

## Unit 5 — Tempo cubeloop-only attribute names

**Files**

- `backend/cubeplex/services/tempo_client.py` — parser, TraceQL,
  search-hit summary: every `cubepi.` vendor key becomes `cubeloop.`.
- `backend/cubeplex/api/routes/v1/admin_traces.py` — tag allowlist and
  `_has_foreign_org_span`.
- `backend/cubeplex/api/schemas/trace.py` — comments only (`cubeloop.turn`).
- `backend/tests/unit/test_tempo_client.py`, `test_tempo_parser.py`
- `backend/tests/fixtures/tempo/` — rewrite `cubepi.*` keys in the JSON
  fixtures to `cubeloop.*`.
- `backend/tests/e2e/test_admin_traces.py` — allowlist tag names.

**Interfaces**

Parser keys (cubeloop only; no cubepi fallback):

```
cubeloop.run_id
cubeloop.metadata.{org,workspace,user,conversation}_id
cubeloop.turn / cubeloop.turn.index / .stop_reason / .tool_calls.count
cubeloop.agent.tools
cubeloop.llm.raw_request / raw_response
cubeloop.tool.is_error / execution_mode
```

`gen_ai.*` is unchanged. Span name `cubepi.turn` is no longer classified
as a turn.

TraceQL org gate (and each optional filter) stays as sibling spansets so
org_id on `invoke_agent` and `gen_ai.request.model` on a chat span can
match independently — only the vendor prefix changes:

```
{ resource.service.name="cubeplex" &&
  span.cubeloop.metadata.org_id="…" }
```

`select(...)` lists the cubeloop spellings.

Tag allowlist is `cubeloop.metadata.{workspace,user,conversation}_id`
plus `gen_ai.request.model`. `_has_foreign_org_span` reads
`cubeloop.metadata.org_id` only.

**Core logic**

String-replace the vendor prefix in the viewer. Do not dual-read, do
not dual-write. CubePlex already passes unprefixed metadata into
`tracing_context`; 0.14 prefixes it `cubeloop.metadata.*`. Pre-0.14
Tempo documents with `cubepi.*` keys stop matching search and fail the
org-id gate on detail (`org_id is None → 404`). That is accepted.

**Tests**

Unit, in `tests/unit/test_tempo_parser.py` / `test_tempo_client.py`
(update the existing assertions; no new dual-namespace cases):

1. Rewritten cubeloop fixtures still parse business ids, turn kind, raw
   request/response.
2. Search TraceQL for an org contains `span.cubeloop.metadata.org_id`
   and does **not** contain `span.cubepi.`.
3. Tag-values allowlist accepts the cubeloop spellings; a `cubepi.*`
   tag and a random tag still 400.
4. `_has_foreign_org_span` flags a child whose
   `cubeloop.metadata.org_id` differs.

E2E `test_admin_traces.py` keeps covering the HTTP 404-on-foreign-org
path with `cubeloop.metadata.org_id`.

---

## Unit 6 — Config, image, gitignore

**Files**

- `backend/config.yaml` — `tracing.directory: "./cubeloop-traces"`;
  comments.
- `backend/cubeplex/agents/tracing.py` — default directory string;
  comments / imports.
- `backend/cubeplex/agents/checkpointer.py` —
  `database.cubeloop_pool_min` / `_max`. No read of the old key.
- `backend/main.py` — mkdir + reload-exclude `cubeloop-traces`.
- `backend/.gitignore` — add `cubeloop-traces/`; keep `cubepi-traces/`.
- `deploy/images/backend/Dockerfile` — `mkdir` `cubeloop-traces`.
- `deploy/kubernetes/scripts/build-and-push.sh` — comment: git+url is
  now cubeloop (GITHUB_MIRROR still rewrites `github.com`).

**Interfaces**

```yaml
tracing:
  directory: "./cubeloop-traces"
```

```
database.cubeloop_pool_min  # default 1
database.cubeloop_pool_max  # default 10
```

No `CUBEPI_*` CubePlex env vars exist today; none are added.
`CUBEPLEX_TRACING__DIRECTORY` (dynaconf) still overrides the yaml path.

**Core logic**

Default directory change only. Operators who already set
`tracing.directory` keep their files. No copy from `cubepi-traces/` to
`cubeloop-traces/`. `cubeloop trace --dir` is how you read leftover
JSONL.

**Tests**

- `tests/unit/agents/test_tracing.py` if it asserts the default path.
- Smoke: `python main.py` in a fresh worktree still starts with reload
  (the mkdir exists so uvicorn does not glob-crash). Not a committed
  test; verified when running the backend.

---

## Unit 7 — Skills and current docs

**Files**

- `skills-lock.json` — source `cubeplexai/cubeloop`, skill paths
  `skills/cubeloop/SKILL.md` and `skills/cubeloop-trace/SKILL.md`, new
  hashes.
- `.agents/skills/cubepi/` and `cubepi-trace/` replaced by upstream
  `cubeloop/` and `cubeloop-trace/` (copy from the cubeloop checkout
  `skills/` tree, or the project's usual skills-lock refresh).
- `AGENTS.md` — backend one-liner; skill index.
- `README.md`, `backend/README.md`
- `backend/docs/agent-system-design.md`,
  `prompt-cache-discipline.md`, `quick-reference.md`
- `docs/testing.md` — "cubepi LLM provider" → cubeloop
- `docs/site/docs/deployment/backend-config.md` + zh-Hans
- `docs/site/docs/deployment/kubernetes.md` + zh-Hans
- `docs/site/docs/guides/conversations/basics.md` + zh-Hans
- Frontend comments that name the package or `cubepi_messages` (wire
  shape is unchanged; comments should not claim a dropped table)

**Interfaces**

None. User-facing strings: `cubeloop trace`, `https://cubeloop.dev`,
`https://github.com/cubeplexai/cubeloop`.

**Core logic**

Current docs only. Frozen `docs/dev/{specs,plans,notes}` are left
alone even when they say cubepi.

**Tests**

None. Docs-only. Grep audit is the check:

```
rg -n "from cubepi|import cubepi" backend/cubeplex backend/tests backend/alembic/env.py
rg -n "cubepi_messages|cubepi_threads|cubepi_runs" backend/cubeplex backend/tests --glob '!alembic/versions/**'
```

Both empty except the documented `eef196f4c8f9` catalog ImportError.

---

## Verification (implementation PR, after all units)

Focused, in order:

1. `uv run pytest tests/unit/test_tempo_parser.py tests/unit/test_tempo_client.py tests/unit/test_alembic_env.py tests/unit/test_run_manager_cubeloop_dict_to_event.py tests/unit/mcp/test_runtime_static_auth.py tests/unit/mcp/test_runtime_tools_cache.py --no-cov`
2. `uv run alembic upgrade head` on the worktree dev DB; psql checks in
   Unit 2.
3. E2E that hit the renamed tables / agent path:
   `tests/e2e/test_cubeloop_checkpointer_integration.py`,
   `test_cubeloop_history_round_trip.py`,
   `test_history_window.py`,
   `test_conversation_fork.py`,
   `test_stranded_run_recovery.py`,
   `test_admin_traces.py`.
4. Pre-push hook runs `make check-ci` on the code push. Do not run
   check-ci by hand first.

---

## Risk register

1. **Autogen against a v5 DB after env.py points metadata at cubeloop.**
   Mitigation: exclude **both** `cubepi_threads` and `cubeloop_threads`
   (and the rest of both prefixes) before generating the v6 revision;
   never apply an autogen that drops `cubepi_*`.
2. **v1 `create_message_partitions_op()` after the pin.** 0.14's helper
   partitions `cubeloop_messages`, which v1 has not created. Mitigation:
   inline `cubepi_messages_pXX PARTITION OF cubepi_messages` in
   `555c11215b57`. Greenfield e2e alembic is the tripwire.
3. **Tempo 0.14 traces invisible if Unit 5 slips.** The attribute rename
   must ship in the same PR as the pin. Pre-0.14 `cubepi.*` traces going
   dark is accepted (no dual-read).
4. **Downgrade that DROPs or skips partitions.** A copy-paste of the
   v4→v5 downgrade shape (`DROP TABLE`) would delete conversation
   history. Parent rename does not rename 128 children. v6 downgrade is
   the full inverse sequence in Unit 2; round-trip on a throwaway DB.
5. **Lockfile churn.** Review `uv.lock` for unrelated bumps before
   commit.
6. **Identifier rename misses a patch string.** Tests that monkeypatch
   `cubeplex.mcp.cubepi_runtime.…` will fail loudly; grep the old module
   path after the git mv.

---

## Out of this plan

Matches the spec: no PyPI-instead-of-git switch, no shim dep, no frozen
docs rewrite, no JSONL directory migration, no dual-read or dual-write
of OTel attrs, no new CubeLoop APIs.
