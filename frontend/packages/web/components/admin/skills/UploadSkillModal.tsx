'use client'

interface UploadSkillModalProps {
  open: boolean
  onOpenChange: (open: boolean) => void
  onUploaded: () => void
}

export function UploadSkillModal(_props: UploadSkillModalProps) {
  return null
}
