'use client'

export function PersonaEditor({ wsId }: { wsId: string }): React.ReactElement {
  return (
    <div className="flex-1 p-8">
      <p className="text-muted-foreground">Persona editor — coming soon. wsId: {wsId}</p>
    </div>
  )
}
