'use client'

export function SkillsPanel({ wsId }: { wsId: string }): React.ReactElement {
  return (
    <div className="flex-1 p-8">
      <p className="text-muted-foreground">Skills panel — coming soon. wsId: {wsId}</p>
    </div>
  )
}
